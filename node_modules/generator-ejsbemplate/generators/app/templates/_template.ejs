<%%
// <%= name %>/_<%= name %>.ejs
// A main HTML file of "<%= name %>" component
//
  this.name = '<%= name %>'; const name = this.name;
  this.ejsRoot = '../../'; const ejsRoot = this.ejsRoot;

  // Un-comment to include content data
  // if (typeof type === 'undefined') { type = 'default'; }
  // include(`./_${name}--data`);
  // this.params = get<%= fName %>Data(type);

  this.params = {
    // Default parameter:
    'modifier': '',
    'text': 'Default text here',
    // 'elem': 'div',
    // 'h': 3,
    // 'head': '<span>Default head text</span>',
    // 'body': '<span>Default body here</span>',
    // 'href': '',
    // 'icon': '',
    // 'items': [{
    //   'elem': 'span',
    //   'modifier': '',
    //   'head': '',
    //   'body': '',
    //   'text': '',
    //   'href': '',
    //   'icon': ''
    // }]
  };
  if (typeof util === 'undefined') { include(ejsRoot + 'util/_util'); util = getUtil(); }
  util.init()(util, this);
<%- end %>%>
<!-- <%%= name %> -->
<div class="<%%= name %><%%- util.setModifier(name, modifier); %>"<%%# util.printAttr('href', href); %>>
  <%%# Head template %>
  <!-- <h<%%# h %>> class="<%%# name %>__head"><%%# head %></h<%%# h %>> -->
  <%%# Loop template %>
  <%%# items.forEach((item) => { %>
      <!-- <span><%%# item.text %></span> -->
  <%%# }); %>
</div>
<!-- /<%%= name %> -->
