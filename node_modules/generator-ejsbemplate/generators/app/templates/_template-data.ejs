<%% get<%= fName %>Data = (key) => {
  const <%= fName %>Data = {
    'default': {
      modifier: '',
    }
  }

  const result = <%= fName %>Data[key];

  if (typeof result === 'undefined') {
    throw new Error('<%= name %>-data: Invalid key name!');
  }

  return result;
}
<%- end %>%>
